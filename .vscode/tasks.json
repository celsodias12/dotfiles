{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "npm",
        "script": "", // Name of your script that is in the package.json
        "group": {
          "kind": "test",
          "isDefault": true
        },
        "label": "", // Your command name (this name is used in the prelaunchtask to run this task)
        "detail": "", // Your command details here
        "isBackground": true,
        "problemMatcher": {
          "owner": "custom", // This is not needed but, required by the problemMatcher Object
          "pattern": {
            "regexp": "^$" // This is not needed but, required by the problemMatcher Object
          },
          "background": {
            "activeOnStart": true,
            "beginsPattern": "Compiling...", // Signals the begin of the Task
            "endsPattern": "Compiled .*" // Signals that now the initialization of the task is complete
          }
        }
      }
    ]
  }
  