{
  "scripts": {
    // react native
    "start": "react-native start --reset-cache",
    "android": "react-native run-android",
    "android:release": "react-native run-android --variant=release",
    "ios": "react-native run-ios",
    "win:clean": "cd android && gradlew clean && cd .. && npm start",
    "win:apk:dev": "cd android && gradlew clean && gradlew assembleRelease --scan && cd app && cd build && cd outputs && cd apk && cd release && start .",
    "win:apk:prod": "cd android && gradlew clean && gradlew bundleRelease && cd app && cd build && cd outputs && cd bundle && cd release && start .",
    "mac:clean": "cd android && ./gradlew clean && cd .. && npm start",
    "mac:apk:dev": "cd android && ./gradlew clean && ./gradlew assembleRelease --scan && cd app && cd build && cd outputs && cd apk && cd release && open .",
    "mac:apk:prod": "cd android && ./gradlew clean && ./gradlew bundleRelease && cd app && cd build && cd outputs && cd apk && cd release && open .",
    "push:android:dev": "appcenter codepush release-react -a RastreagroTests/Transportes-Android -d Staging",
    "push:android:prod": "appcenter codepush release-react -a RastreagroTests/Transportes-Android -d Production",
    "test": "jest",
    "test:deepLink": "adb shell am start -W -a android.intent.action.VIEW -d 'rastreagro://transportes?Home=true/test=false' com.rastreagro",
    "lint": "eslint src/** --ext .js,.jsx,.ts,.tsx && npx tspsc",
    "lint:fix": "eslint src/** --ext .js,.jsx,.ts,.tsx --fix",
    "check:versions": "npx solidarity",
    "fastDeleteNodeModules": "npx npkill",
    "react-devtools": "react-devtools",
    "bundle": "npx react-native-bundle-visualizer"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "npm lint:fix"
    ]
  },
}
