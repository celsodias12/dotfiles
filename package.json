{
  "scripts": {
    // react native
    "start": "react-native start --reset-cache",
    "android": "react-native run-android",
    "android:release": "adb uninstall com.rastreagro & react-native run-android --variant=release",
    "ios": "react-native run-ios",
    "adb": "adb reverse tcp:9090 tcp:9090 && adb reverse tcp:3000 tcp:3000 && adb reverse tcp:9001 tcp:9001 && adb reverse tcp:8081 tcp:8081",
    "clean": "run-script-os",
    "apk_dev": "run-script-os",
    "apk_prod": "run-script-os",
    "apk_bundle": "run-script-os",
    "bundle:ios": "react-native bundle --entry-file index.js --platform ios --dev false --bundle-output ios/main.jsbundle --assets-dest ios && npm run apk_bundle",
    "bundle:android": "react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res && npm run apk_bundle",
    "push:android:dev": "appcenter codepush release-react -a RastreagroTests/Transportes-Android -d Staging",
    "push:android:prod": "appcenter codepush release-react -a RastreagroTests/Transportes-Android -d Production",
    "test": "jest",
    "test:deepLink": "adb shell am start -W -a android.intent.action.VIEW -d 'rastreagro://transportes?Home=true/test=false' com.rastreagro",
    "test:versions": "npx solidarity",
    "lint": "eslint src/** --ext .js,.jsx,.ts,.tsx && npx tsc",
    "lint:fix": "eslint src/** --ext .js,.jsx,.ts,.tsx --fix",
    "react-devtools": "react-devtools",
    "bundle": "npx react-native-bundle-visualizer",
    "fastDeleteNodeModules": "npx npkill",
    "postinstall": "node ./src/bin/postInstall",
    "setup": "node ./src/bin/setup",
    "clean:nix": "react-native-clean-project",
    "clean:win32": "cd android && gradlew clean && cd .. && npm start",
    "apk_dev:nix": "cd android && ./gradlew clean && ./gradlew assembleRelease --scan && cd app && cd build && cd outputs && cd apk && cd release && open .",
    "apk_dev:win32": "cd android && gradlew clean && gradlew assembleRelease --scan && cd app && cd build && cd outputs && cd apk && cd release && start .",
    "apk_prod:nix": "cd android && ./gradlew clean && ./gradlew bundleRelease && cd app && cd build && cd outputs && cd bundle && cd release && open .",
    "apk_prod:win32": "cd android && gradlew clean && gradlew bundleRelease && cd app && cd build && cd outputs && cd bundle && cd release && start .",
    "apk_bundle:nix": "cd android && ./gradlew clean && ./gradlew assembleDebug && cd app && cd build && cd outputs && cd apk && cd debug && open .",
    "apk_bundle:win32": "cd android && gradlew clean && gradlew assembleDebug && cd app && cd build && cd outputs && cd apk && cd debug && start ."
  },
  "engines": {
    "yarn": "ONLY THE USE OF NPM IS ALLOWED", // For this to work add the .npmrc file
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "npm lint:fix"
    ]
  },
}
