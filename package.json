{
  "scripts": {
    "android:release": "react-native run-android --variant=release",
    "win:clean": "cd android && gradlew clean && cd .. && npm start -- --reset-cache",
    "mac:clean": "cd android && ./gradlew clean && cd .. && npm start -- --reset-cache",
    "win:build:android:dev": "cd android && gradlew assembleRelease && cd app && cd build && cd outputs && cd apk && cd release && start .",
    "win:build:android:prod": "cd android && gradlew bundleRelease && cd app && cd build && cd outputs && cd bundle && cd release && start .",
    "mac:build:android:dev": "cd android && ./gradlew assembleRelease && cd app && cd build && cd outputs && cd apk && cd release && open .",
    "mac:build:android:prod": "cd android && ./gradlew bundleRelease && cd app && cd build && cd outputs && cd bundle && cd release && open .",
    "push:android:dev": "appcenter codepush release-react -a username/appName -d Staging",
    "push:android:prod": "appcenter codepush release-react -a username/appName -d Production",
    "start:production": "NODE_ENV=production react-native start",
    "lint": "eslint src/** --no-error-on-unmatched-pattern",
    "lint:fix": "eslint src/** --fix --no-error-on-unmatched-pattern"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "yarn lint:fix"
    ]
  },
}
